# Copy this file to .env and adjust values for the target environment.
# Never commit the real .env file.

APP_NAME=container-app
APP_VERSION=latest

# Replace these with the ports required by the project.
HOST_PORT=TODO_HOST_PORT
CONTAINER_PORT=TODO_CONTAINER_PORT

# Optional generic runtime settings.
DATA_DIR=/data
LOG_LEVEL=info
